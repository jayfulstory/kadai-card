// const title = document.querySelector('#title');

// window.addEventListener('scroll', () => {
//   if (`${scrollY / 10 + 1}` < 60) {
//     title.style.transform = `scale(${scrollY / 10 + 1})`;
//   }
// });
